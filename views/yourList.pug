include _functions

h3 Your List of Movies
ul
  if (savedList)
    each movie in savedList
      li 
        p #{movie.title} #{format(movie.release_date)}
        button(class='deleteMovie' value=''+ movie.id) Delete Movie   

  else
    li(class='faded') Example


